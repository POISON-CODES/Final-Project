import 'package:flutter/material.dart';

class HomePageTab extends StatelessWidget {
  const HomePageTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Home Page Tab"),
    );
  }
}
